name: Generate previews deprecated

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deprecated. Use the Generate SEO articles workflow instead."